https://kinsta.com/es/blog/gmail-smtp-servidor/

cuenta de prueba 
formularioenvio@gmail.com
R123456789mu 




----SMTP--------

use PHPMailer\PHPMailer\PHPMailer;
use PHPMailer\PHPMailer\SMTP;
$result="";

require '../views/phpmailer/src/PHPMailer.php';
require '../views/phpmailer/src/SMTP.php';
require '../views/phpmailer/src/Exception.php';
require '../views/phpmailer/src/OAuth.php';

$mail = new PHPMailer();

/** Configurar SMTP para el acceso de la cuenta gmail**/
$mail->isSMTP();                                      // Indicamos que use SMTP
$mail->Host = "smtp.gmail.com";                        // Indicamos los servidores SMTP
$mail->SMTPAuth = true;                               // Habilitamos la autenticación SMTP
$mail->Username = 'formularioenvio@gmail.com';                 // SMTP username
$mail->Password = 'R123456789mu';                           // SMTP password
$mail->SMTPSecure = 'tls';                            // Habilitar encriptación TLS o SSL
$mail->Port = 587;                                    // TCP port

/** Configurar cabeceras del mensaje **/
$mail->From = 'formularioenvio@gmail.com';                       // Correo del remitente
$mail->FromName = 'Robertclasing';           // Nombre del remitente
$mail->Subject = 'test de asunto';                // Asunto

/** Incluir destinatarios. El nombre es opcional **/
$mail->addAddress('robertclasing@gmail.com');


/** Enviarlo en formato HTML **/
$mail->isHTML();                                  

/** Configurar cuerpo del mensaje **/
$mail->Body    = 'Este es el mensaje en HTML <b>en negrita!</b>';

/** Para que use el lenguaje español **/
$mail->setLanguage('es');

/** Enviar mensaje... **/
if(!$mail->send()) {
    echo 'El mensaje no pudo ser enviado.';
    echo 'Mailer Error: ' . $mail->ErrorInfo;
} else {
    echo 'Mensaje enviado correctamente';
}



----Formulario2----


$recipient = "robertclasing@gmail.com";
$subject = "Informacion";
$location = "/";
$sender = "robertclasing1@gmail.com";
$body = "Nombre: Hola \n";
$body .= "Email: robertclasing1@gmail.com \n";
$body .= "Mensaje: Holas222 \n";
$headers = "From: Hola <robertclasing1@gmail.com>\r\n";
mail( $recipient, $subject, $body, $headers ) or die ("Mail could not be sent.");